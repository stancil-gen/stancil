// Code generated by stencil. DO NOT EDIT.
package {{ .Package }}

import (
	"context"
	"fmt"
{{ range .Imports }}	"{{ . }}"
{{ end }})
{{ range .Mappers }}
{{ $mapper := . }}
// {{ $mapper.InterfaceName }} defines the mapper contract for the {{ $mapper.APIName }} API.
// Each Map*Input method builds the step input from the shared context.
// MapResponse assembles the final response DTO (returns shared.Response if set by BeforeResponse hook).
type {{ $mapper.InterfaceName }} interface {
{{ range $mapper.Methods }}	{{ .Name }}(ctx context.Context, shared *{{ $mapper.ContextType }}) ({{ .ReturnType }}, error)
{{ end }}}

// {{ $mapper.DefaultImplName }} provides default implementations.
// Fields that match by name+type between request and model are auto-mapped.
// Fields marked "cannot infer" must be set in the corresponding Before hook.
type {{ $mapper.DefaultImplName }} struct{}
{{ range $mapper.Methods }}
func (m *{{ $mapper.DefaultImplName }}) {{ .Name }}(ctx context.Context, shared *{{ $mapper.ContextType }}) ({{ .ReturnType }}, error) {
{{ .Body }}
}
{{ end }}{{ end }}
